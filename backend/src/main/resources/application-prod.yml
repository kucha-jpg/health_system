server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/health_system_prod?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: prod_user
    password: prod_password
    driver-class-name: com.mysql.cj.jdbc.Driver

jwt:
  secret: ${JWT_SECRET:PLEASE_CHANGE_IN_PRODUCTION}
  expiration: ${JWT_EXPIRATION:86400000}
