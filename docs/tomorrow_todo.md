# 明日继续开发待办

## 当前状态确认
- Docker 服务已成功启动：mysql、backend、frontend 均为 Up 状态。
- 端口映射正常：
  - backend: 9090
  - frontend: 80
  - mysql: 3306

## 已完成功能（昨天）
- 管理员反馈统计卡片。
- 近 7 天反馈趋势图。
- 按筛选条件导出 CSV（含中文兼容处理）。

## 下一步优先任务
1. 反馈批量处理
- 管理端反馈表格支持多选。
- 增加“批量标记已处理/未处理”按钮。
- 后端增加批量更新状态接口。
- 前端操作后刷新：统计卡片、趋势图、列表。

2. 交互与安全细节
- 批量操作二次确认弹窗。
- 对空选择做提示（请先选择数据）。
- 批量结果提示：成功条数/失败条数。

3. 回归验证清单
- 分页 + 日期筛选 + 关键字筛选仍正常。
- 导出 CSV 与当前筛选一致。
- 统计卡片与趋势图数据刷新正常。
- 非管理员页面（我的反馈）不受影响。

## 启动与验证命令
1. 启动服务
- docker compose up -d

2. 查看状态
- docker compose ps

3. 若改代码后重新构建
- docker compose up -d --build backend frontend

## 明日建议执行顺序
1. 先做后端批量状态接口。
2. 再做前端表格多选与批量按钮。
3. 最后做接口烟测 + 页面验收。
